스마트모빌리티공학실험2 Final Project

이름: 권민지

학번: 12241991

알고리즘명: PCA와 가중 KNN을 이용한 RTT 기반 위치추정 알고리즘
1. 동기 및 개요

목표는 18개 기지국에서 측정된 RTT 값을 이용하여 사용자의 2차원 위치를 추정하는 것이다. 제공된 데이터는 사용자 1명에 대해 18개의 RTT 측정값을 가지고 최종 출력은 해당 사용자의 위치 좌표인 x, y 값이다. 따라서 해결할 문제는 18차원 RTT 입력 벡터를 2차원 위치 좌표로 변환하는 회귀 기반 위치추정 문제로 볼 수 있다.

초기에는 RTT 기반 위치추정에서 자주 사용되는 삼각측량, 최소제곱법, RANSAC 기반 이상치 제거 방식을 고려하였다. 그러나 RANSAC이나 WLS 기반 접근은 위치추정 문제에서 일반적으로 많이 사용될 가능성이 높다고 판단하였다. 즉 단순한 수식 기반 위치추정 방식보다 RTT 패턴 자체를 활용하는 방식이 더 차별성이 있을 것같다고 판단했다.

이에 따라 본 프로젝트에서는 18개 RTT 값을 하나의 fingerprint 형태로 보고 현재 사용자의 RTT 패턴과 유사한 학습 샘플을 찾아 위치를 추정하는 PCA-assisted Weighted KNN 방식을 생각하였다. 이 방식은 기존 fingerprint 기반 실내 측위에서 사용되는 KNN/WKNN 아이디어를 RTT 데이터에 적용한 것이다. 특히 18차원 RTT를 그대로 비교하지 않고 PCA를 이용해 주요 RTT 변화 패턴을 저차원 특징으로 압축한 뒤에 Weighted KNN을 이용해 위치를 추정하도록 구성하였다.

알고리즘의 전체 구조는 아래와 같은 순서로 구성된다.

1. 한 사용자에 대한 18개 RTT 값 입력
2. 기지국별 RTT scale 차이를 줄이기 위한 정규화
3. 18차원 RTT를 3차원 주요 특징으로 압축
4. PCA 공간에서 유사한 학습 샘플 탐색
5. 가까운 이웃에 더 큰 가중치를 부여하여 위치 추정

최종적으로 validation 결과를 바탕으로 PCA 차원은 3, KNN 이웃 수는 5로 설정하는것이 가장 낫다고 판단했다.

2. 알고리즘 설명

이번 프로젝트에서 사용한 입력 벡터는 한 사용자에 대해 18개 기지국에서 측정된 RTT 값이다. 이를 다음과 같이 정의한다.

d = [d₁, d₂, ..., d₁₈]ᵀ

여기서 dᵢ는 i번째 기지국에서 측정된 RTT 값이다. 학습 데이터 전체에 대해 각 RTT feature의 평균과 표준편차를 계산하고, 입력 RTT 벡터를 다음과 같이 정규화하였다.

z = (d - μ) / σ

여기서 μ는 학습 데이터에서 계산한 feature별 평균이고 σ는 feature별 표준편차이다. 정규화를 수행한 이유는 각 기지국 RTT 값의 분포가 서로 다를 수 있기 때문에 특정 기지국의 값이 거리 계산에 과도하게 영향을 주는 것을 줄이기 위해서다.

정규화된 18차원 RTT 벡터는 PCA를 통해 저차원 특징 벡터로 변환하였다. 학습 데이터의 정규화된 RTT 행렬에 대해 PCA 주요 성분을 계산하고 validation 결과를 바탕으로 상위 3개의 principal component를 사용하였다. 이를 수식으로 나타내면 다음과 같다.

q = V₃ᵀz

여기서 V₃는 학습 데이터에서 얻은 PCA 주요 성분 3개로 구성된 변환 행렬이고 q는 PCA 공간에서의 3차원 특징 벡터이다. PCA를 사용한 이유는 18개의 RTT 값 중 서로 중복되거나 잡음이 포함된 정보를 줄이고 전체 RTT 패턴을 대표하는 주요 특징을 이용하기 위해서이다.

새로운 사용자 입력이 들어오면 해당 입력도 같은 평균 μ와 표준편차 σ를 이용하여 정규화하고 같은 PCA 변환 행렬 V₃를 이용해 3차원 특징 공간으로 변환한다. 이후 학습 데이터의 PCA 특징 qⱼ와 현재 사용자 특징 q 사이의 유클리드 거리를 계산한다.

Dⱼ = ||q - qⱼ||₂

여기서 Dⱼ는 현재 사용자와 j번째 학습 샘플 사이의 PCA 공간 거리이다. 모든 학습 샘플에 대해 Dⱼ를 계산한 뒤에 거리가 가장 작은 K개의 샘플을 이웃으로 선택한다. 이번  프로젝트에서는 validation 결과를 바탕으로 K = 5를 사용하였다.

기본 KNN은 선택된 이웃들의 위치를 단순 평균하지만 본 프로젝트에서는 가까운 이웃이 더 큰 영향을 갖도록 Weighted KNN을 사용하였다. 가중치는 PCA 공간 거리의 역수 형태로 정의하였다.

wⱼ = 1 / (Dⱼ + ε)

여기서 ε은 거리가 0이 되는 경우 분모가 0이 되는 것을 방지하기 위한 작은 상수이다. 최종 위치는 선택된 K개 이웃의 실제 위치 pⱼ를 가중 평균하여 계산하였다.

p_hat = Σ wⱼpⱼ / Σ wⱼ

이때 p_hat은 예측된 사용자 위치이고 pⱼ는 j번째 이웃 샘플의 실제 위치이다.

추가적으로 PCA 공간에서 가까운 샘플이 원래 RTT 공간에서도 항상 가장 유사하다고 보장할 수는 없기 때문에 원래 정규화 RTT 공간에서의 residual도 함께 검토하였다. RTT residual은 다음과 같이 정의할 수 있다.

Rⱼ = ||z - zⱼ||₂

이 값을 이용하면 PCA 공간 거리뿐 아니라 원래 RTT 패턴의 유사성도 함께 반영할 수 있다. 따라서 개선된 가중치 구조는 다음과 같이 정의할 수 있다.

wⱼ = 1 / ((Dⱼ + ε)(Rⱼ + ε))

다만 validation 결과에서는 PCA 차원 3과 K = 5 조합이 가장 낮은 평균 오차를 보였고 최종 모델에서는 이 설정을 기준으로 학습된 model.pkl을 생성하였다. 최종 main.py는 학습을 수행하지 않고 train.py에서 저장한 model.pkl을 불러와 hidden test data에 대해 추론만 수행하도록 구성하였다.

3. Agent AI 활용

본 프로젝트에서는 Agent AI를 단순히 코드를 자동 생성하는 용도로만 사용하지 않고 알고리즘 후보를 비교하고 문제점을 분석하며 보고서 구조를 정리하는 보조 도구로 활용하였다.

구현 과정에서도 Agent AI를 활용하였다. 처음에는 train.py에서 학습한 700개 전체 데이터를 main.py에서 다시 평가하였기 때문에 PCA 차원과 K값을 바꾸어도 평균 오차가 거의 0에 가깝게 동일하게 나타났다. 이 문제에 대해 Agent AI와 함께 원인을 분석한 결과로 KNN 특성상 학습 데이터와 평가 데이터가 같을 경우 자기 자신을 가장 가까운 이웃으로 선택하기 때문에 성능이 과대평가된다는 점을 확인하였다. 이에 따라 별도의 validate.py를 작성하여 700개 데이터를 학습용 560개와 검증용 140개로 나누고 학습에 사용하지 않은 데이터에 대해 성능을 평가하도록 수정하였다.

따라서 본 프로젝트에서 Agent AI는 알고리즘을 대신 결정한 것이 아니라 오류 원인 분석, validation 방식 설계, 보고서 문장 구조화에 활용되었다. 최종 알고리즘 선택과 validation 결과 해석, 최종 하이퍼파라미터 결정은 직접 실험 결과를 확인한 뒤 수행하였다.

4. 결과 도출 및 해석

본 프로젝트에서는 제공된 700개 데이터를 무작위로 섞은 뒤 80%는 학습 데이터로 사용하고 20%는 validation 데이터로 사용하였다. 즉 전체 700개 중 560개는 PCA 변환 행렬과 KNN reference database를 구성하는 데 사용하였고 나머지 140개는 학습에 사용하지 않은 검증 데이터로 두었다. 이 방식은 KNN이 자기 자신을 이웃으로 선택하는 문제를 방지하고 hidden test set과 유사한 조건에서 자체 평가를 수행하기 위한 목적이다.

평가 지표로는 평균 오차, 중앙값 오차, 최대 오차를 사용하였다. 오차는 예측 위치 p_hat과 실제 위치 p 사이의 유클리드 거리로 계산하였다.

e = ||p_hat - p||₂

PCA 차원과 KNN 이웃 수를 바꾸어 총 5개의 실험을 수행하였다.

Experiment	PCA Dimension	K Neighbors	Mean Error	Median Error	Max Error
1	3	5	9.708671	8.076405	34.118211
2	4	5	10.199847	9.067473	28.369445
3	5	7	10.086319	9.252091	38.254690
4	6	7	9.989855	8.957825	38.004337
5	8	9	10.067692	8.974367	34.707080

실험 결과는 PCA 차원 3과 KNN 이웃 수 5를 사용한 실험 1에서 평균 오차가 9.708671로 가장 낮게 나타났다. 따라서 최종 모델에서는 PCA dimension = 3, K = 5를 사용하였다.

PCA 차원이 너무 작으면 18개 RTT 값에 포함된 위치 관련 정보가 과도하게 손실될 수 있다. 반대로 PCA 차원이 너무 크면 주요 패턴뿐 아니라 잡음 성분까지 함께 반영될 가능성이 있다. 본 실험에서는 PCA 차원 3이 가장 낮은 평균 오차를 보였다. 이는 18차원 RTT 전체를 모두 사용하는 것보다 주요 특징만 남기는 것이 validation 성능에 더 적합했음을 의미한다.

KNN 이웃 수 역시 성능에 영향을 주었다. K가 너무 작으면 특정 샘플 하나에 민감해질 수 있고 K가 너무 크면 실제 위치와 다소 먼 샘플까지 평균에 포함되어 위치 추정 결과가 흐려질 수 있다. 본 실험에서는 K = 5일 때 가장 낮은 평균 오차가 나타났다.

main.py를 실행했을 때 전체 700개 데이터에 대한 오차가 거의 0에 가깝게 나타났지만 이 값은 최종 성능으로 사용하지 않았다. 그 이유는 model.pkl이 전체 700개 데이터를 이용해 생성되었고 main.py에서 같은 700개 데이터를 다시 예측했기 때문이다. KNN 기반 알고리즘에서는 평가 샘플이 학습 database에 포함되어 있으면 자기 자신을 가장 가까운 이웃으로 선택할 수 있다. 따라서 main.py 결과는 코드 실행 형식이 정상인지 확인하는 용도로만 사용하고 실제 성능 비교는 validation split 결과를 기준으로 수행하였다.

본 알고리즘의 장점은 구현이 단순하면서도 hidden test set에 대해 비교적 안정적으로 동작할 수 있다는 점이다. 복잡한 딥러닝 모델은 700개라는 제한된 학습 데이터에서 과적합될 가능성이 크지만 PCA-WKNN 방식은 학습 데이터의 RTT 패턴을 직접 활용하므로 데이터 수가 적은 상황에서도 안정적인 선택이 될 수 있다. 또한 PCA를 통해 차원을 줄이기 때문에 18차원 RTT 전체를 그대로 비교하는 KNN보다 잡음과 중복 feature의 영향을 줄일 수 있다.

반면 한계도 존재한다. PCA는 위치 정답 p를 직접 고려하지 않고 RTT 데이터의 분산이 큰 방향을 기준으로 특징을 추출한다. 따라서 PCA에서 보존한 주성분이 반드시 위치 오차를 최소화하는 방향이라고 보장할 수는 없다. 또한 Weighted KNN은 학습 데이터 분포에 의존하므로 hidden test set이 제공 데이터의 분포와 크게 다를 경우 성능이 저하될 수 있다. 향후에는 PCA 대신 위치 오차를 직접 고려하는 supervised dimensionality reduction이나, anchor별 신뢰도를 반영하는 adaptive weighting 방식을 추가로 적용해볼 수 있다.

5. Reference

[1] Shiwu Xu, Chih-Cheng Chen, Yi Wu, Xufang Wang, and Fen Wei, “Adaptive Residual Weighted K-Nearest Neighbor Fingerprint Positioning Algorithm Based on Visible Light Communication,” Sensors, 2020.
https://pubmed.ncbi.nlm.nih.gov/32784420/

이 논문에서는 fingerprint positioning 문제에서 Weighted KNN을 사용할 때 단순히 가까운 이웃을 선택하는 것뿐만 아니라 residual 정보를 함께 고려하여 위치추정의 신뢰도를 높이는 방법을 제안한다. 이번 프로젝트에서 이 논문을 통해 “현재 측정값과 학습 데이터 사이의 차이를 단순 거리로만 보지 않고, residual 관점에서 해석할 수 있다”는 점을 참고하였다. 이를 바탕으로 18개 기지국의 RTT 값을 하나의 fingerprint로 보고 PCA 공간에서 가까운 이웃을 선택한 뒤 원래 RTT 공간에서의 차이도 함께 고려할 수 있는 구조를 검토하였다. 하지만 위 논문은 visible light communication 환경에서 fingerprint를 구성했지만 이번 스마트모빌리티 공학 실험 프로젝트는 RTT 기반 데이터를 사용하였다는 점이 다르다. 또한 이번 프로젝트에서는 복잡한 residual 보정 모델을 그대로 구현하기보다 과제 데이터 크기와 실행 시간 제한을 고려하여 PCA-WKNN 구조 안에서 residual 개념을 해석하고 적용하는 방향으로 활용하였다.

[2] T. H. Duong et al., “Efficient and Accurate Indoor Positioning System: A Hybrid Approach Integrating PCA, WKNN, and Linear Regression,” Journal of Communications, 2024.
https://www.jocm.us/2024/JCM-V19N1-37.pdf

이 연구는 실내 위치추정에서 PCA와 WKNN을 결합하여 fingerprint 데이터의 차원을 줄이고 가까운 이웃의 위치 정보를 이용해 위치를 추정하는 방법을 다루고 있다. 본 프로젝트에서는 이 논문을 통해 PCA가 단순한 데이터 압축이 아니라 위치추정에 사용되는 fingerprint feature의 잡음과 중복성을 줄이는 전처리 과정으로 활용될 수 있다는 점을 참고하였다. 특히 18개 RTT 값을 그대로 KNN에 넣는 경우 모든 기지국의 측정값이 동일한 중요도를 갖게 되므로, PCA를 이용해 주요 RTT 변화 패턴을 먼저 추출하는 방향을 설정하는 데 도움을 받았다. 다만 기존 연구는 PCA, WKNN, Linear Regression을 함께 사용하는 hybrid 구조인 반면, 본 프로젝트에서는 제공 데이터가 700개로 제한되어 있고 main.py 실행 시간이 10분으로 제한되어 있으므로 PCA와 Weighted KNN만을 사용하여 더 단순하고 안정적인 구조로 구현하였다. 또한 기존 연구는 주로 Wi-Fi RSS fingerprint 기반 접근인 반면, 본 프로젝트에서는 18개 기지국의 RTT 벡터를 fingerprint로 사용했다는 점에서 차이가 있다.

[3] J. Luo et al., “A Smartphone Indoor Localization Algorithm Based on WLAN Location Fingerprinting with Feature Extraction and Clustering,” Sensors, 2017.
https://www.mdpi.com/1424-8220/17/6/1339?utm_source=chatgpt.com

이 논문은 WLAN fingerprint 기반 실내 위치추정에서 feature extraction과 fingerprint 데이터 처리 과정이 위치추정 성능에 영향을 줄 수 있음을 보여준다. 본 프로젝트에서는 이 논문을 참고하여, 모든 RTT 값을 그대로 사용하는 것보다 위치추정에 의미 있는 특징을 먼저 추출하는 과정이 필요하다고 판단하였다. 특히 제공된 데이터는 사용자 1명당 18개의 RTT 값을 가지므로, 이 18차원 입력에 포함된 중복 정보나 잡음의 영향을 줄이기 위해 PCA 기반 feature extraction을 적용하는 방향을 정하는 데 도움을 받았다. 다만 해당 연구는 스마트폰 WLAN RSS fingerprint를 대상으로 하고 clustering 과정도 포함하지만, 본 프로젝트에서는 고정된 18개 기지국의 RTT 값을 사용하며 clustering 대신 PCA 공간에서 Weighted KNN을 수행하였다. 따라서 본 프로젝트는 이 연구의 feature extraction 필요성은 참고하되, 데이터 구조와 과제 조건에 맞게 더 단순한 PCA-WKNN 구조로 변형하였다.

[4] 오종택, “WiFi 핑거프린트 위치추정 방식에서 W-KNN의 가중치에 관한 연구,” 지능정보논문지, 2017.
https://www.dbpia.co.kr/journal/articleDetail?nodeId=NODE12205308

이 연구는 Wi-Fi fingerprint 위치추정에서 Weighted KNN의 가중치 설정이 위치추정 성능에 영향을 줄 수 있다는 점을 다룬다. 본 프로젝트에서는 이 연구를 통해 KNN에서 선택된 이웃을 모두 동일하게 평균하는 것보다, 현재 측정값과 더 가까운 이웃에 더 큰 가중치를 부여하는 것이 합리적이라는 점을 참고하였다. 이를 바탕으로 본 프로젝트에서는 PCA 공간에서 현재 RTT 특징과 학습 샘플 사이의 거리를 계산하고, 거리가 가까운 이웃일수록 더 큰 가중치를 갖도록 위치를 추정하였다. 기존 연구는 Wi-Fi fingerprint 환경에서 W-KNN 가중치 방식을 분석한 반면, 본 프로젝트는 RTT 기반 데이터에 PCA feature extraction을 먼저 적용한 뒤 Weighted KNN을 수행했다는 점에서 차이가 있다. 즉, 본 프로젝트에서는 해당 연구의 “가까운 fingerprint에 더 큰 신뢰도를 부여한다”는 핵심 아이디어를 RTT 기반 위치추정 문제에 맞게 변형하여 사용하였다.
